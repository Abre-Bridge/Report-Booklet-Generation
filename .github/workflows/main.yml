name: Flutter Android Build

on:
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build-android:
    name: Build Android APK
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: "stable"
          cache: true

      # ---------------------------
      # Cache dependencies
      # ---------------------------
      - name: Cache pub dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.pub-cache
            .dart_tool
          key: pub-${{ runner.os }}-${{ hashFiles('pubspec.lock') }}
          restore-keys: pub-${{ runner.os }}-

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('android/gradle/wrapper/gradle-wrapper.properties') }}
          restore-keys: gradle-${{ runner.os }}-

      # ---------------------------
      # ENV FILE (root)
      # ---------------------------
      - name: Create .env file
        run: |
          touch .env
          [ -n "${{ secrets.API_BASE_URL }}" ] && echo "API_BASE_URL=${{ secrets.API_BASE_URL }}" >> .env || true
          [ -n "${{ secrets.SUPABASE_URL }}" ] && echo "SUPABASE_URL=${{ secrets.SUPABASE_URL }}" >> .env || true
          [ -n "${{ secrets.SUPABASE_ANON_KEY }}" ] && echo "SUPABASE_ANON_KEY=${{ secrets.SUPABASE_ANON_KEY }}" >> .env || true

      # ---------------------------
      # Firebase / Google Services
      # ---------------------------
      - name: Write google-services.json
        run: |
          if [ -n "$GSVC" ]; then
            echo "$GSVC" > android/app/google-services.json
            echo "google-services.json written."
          else
            echo "No GOOGLE_SERVICES_JSON secret — skipping."
          fi
        env:
          GSVC: ${{ secrets.GOOGLE_SERVICES_JSON }}

      # ---------------------------
      # Keystore
      # ---------------------------
      - name: Decode and place keystore
        run: |
          if [ -n "$KS_B64" ]; then
            echo "$KS_B64" | base64 --decode > android/app/keystore.jks
            printf 'storePassword=%s\nkeyPassword=%s\nkeyAlias=%s\nstoreFile=keystore.jks\n' \
              "${{ secrets.STORE_PASSWORD }}" \
              "${{ secrets.KEY_PASSWORD }}" \
              "${{ secrets.KEY_ALIAS }}" \
              > android/key.properties
            echo "Keystore written."
          else
            echo "Building unsigned."
          fi
        env:
          KS_B64: ${{ secrets.KEYSTORE_BASE64 }}

      # ---------------------------
      # Flutter build steps
      # ---------------------------
      - name: Flutter doctor
        run: flutter doctor -v
        continue-on-error: true

      - name: Install dependencies
        run: flutter pub get

      - name: Run build_runner (if used)
        run: |
          if grep -q "build_runner" pubspec.yaml; then
            dart run build_runner build --delete-conflicting-outputs
          else
            echo "build_runner not in pubspec — skipping."
          fi

      - name: Analyze code
        run: flutter analyze --no-fatal-infos --no-fatal-warnings
        continue-on-error: true

      - name: Build debug APK
        run: flutter build apk --debug --verbose

      - name: Build release APK
        run: flutter build apk --release --verbose

      # ---------------------------
      # Artifacts
      # ---------------------------
      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: debug-apk-${{ github.sha }}
          path: build/app/outputs/flutter-apk/app-debug.apk
          retention-days: 7

      - name: Upload release APK
        uses: actions/upload-artifact@v4
        with:
          name: release-apk-${{ github.sha }}
          path: build/app/outputs/flutter-apk/app-release.apk
          retention-days: 30
          if-no-files-found: warn

      - name: Report APK sizes
        if: always()
        run: |
          find build/app/outputs/flutter-apk/ -name "*.apk" \
            -exec sh -c 'echo "$(basename {}) — $(du -sh {} | cut -f1)"' \;
